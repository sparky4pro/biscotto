#import "std/print"


main :: fn () s32 {
	print("----------------------------------------\n");

	// a := [_]s32.{ 10, 20 };
	a := []s32.{};
	// b := [_]s32.{ 30, 40 };
	b := []s32.{};

	x := join_slices(a, b);
	defer free_slice(&x);

	print("%\n", x);

	print("DONE!\n");
	print("----------------------------------------\n");
	return 0;
}
