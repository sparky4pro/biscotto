main :: fn () s32 {
	#if PLATFORM != .WINDOWS {
		panic("This example is awailable only on Windows, sorry.");
	}
	exe := add_executable("window");
	add_unit(exe, "main.bl");
	compile(exe);

	return 0;
}
